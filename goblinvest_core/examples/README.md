# Examples

Each subfolder is a self-contained demo. Run the scripts from the package
root with `uv run` so the package environment is picked up.

| Demo                                       | What it shows                                                             |
|--------------------------------------------|---------------------------------------------------------------------------|
| [`load_transactions/`](load_transactions/) | End-to-end: build a vault, load CSV statements, fetch prices, summarize.  |
